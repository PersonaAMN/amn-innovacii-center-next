import { ButtonProps } from "@/types/common.types";

const CustomButton = ({
  title,
  handleClick,
  isSubmitting,
  type,
  bgColor,
  textColor,
}: ButtonProps) => {
  return <button>button</button>;
};

export default CustomButton;
